# Proyecto 1: Analizador Lexico

## Para ejecutar el backend
En la consola:

`cd bck`

Después:

`pip install -r requirements.txt`

En la base del proyecto:

`python bck/main.py`

o, estando dentro de `bck`:

`python main.py`